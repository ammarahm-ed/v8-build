diff --git a/BUILD.gn b/BUILD.gn
index 1e3f0181dfb..9d1583f5ac8 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -1154,6 +1154,16 @@ config("cppgc_header_features") {
   }
 }
 
+config("android15_16kb_support") {
+  ldflags = []
+  if (v8_current_cpu == "x64" || v8_current_cpu == "arm64") {
+    ldflags += [
+      "-Wl,-z,max-page-size=16384",
+      "-Wl,-z,common-page-size=16384",
+    ]
+  }
+}
+
 enabled_external_defines =
     enabled_external_v8_defines + enabled_external_cppgc_defines
 disabled_external_defines =
@@ -6477,7 +6487,7 @@ v8_source_set("torque_ls_base") {
   ]
 }
 
-v8_component("v8_libbase") {
+v8_source_set("v8_libbase") {
   sources = [
     "src/base/abort-mode.cc",
     "src/base/abort-mode.h",
@@ -6809,7 +6819,7 @@ if (v8_use_libm_trig_functions) {
   }
 }
 
-v8_component("v8_libplatform") {
+v8_source_set("v8_libplatform") {
   sources = [
     "include/libplatform/libplatform-export.h",
     "include/libplatform/libplatform.h",
diff --git a/src/base/build_config.h b/src/base/build_config.h
index 207f46b057d..48436737083 100644
--- a/src/base/build_config.h
+++ b/src/base/build_config.h
@@ -96,8 +96,8 @@ constexpr int kMinimumOSPageSize = 16 * 1024;
 // pages.
 constexpr int kMinimumOSPageSize = 64 * 1024;
 #else
-// Everything else uses 4kB pages.
-constexpr int kMinimumOSPageSize = 4 * 1024;
+// Everything else uses 16kB pages.
+constexpr int kMinimumOSPageSize = 16 * 1024;
 #endif
 
 #endif  // V8_BASE_BUILD_CONFIG_H_
diff --git a/src/builtins/builtins-typed-array.cc b/src/builtins/builtins-typed-array.cc
index b2cd7e53315..4d8b1983aae 100644
--- a/src/builtins/builtins-typed-array.cc
+++ b/src/builtins/builtins-typed-array.cc
@@ -820,9 +820,15 @@ BUILTIN(Uint8ArrayPrototypeToBase64) {
 
     size_t simd_result_size;
     if (uint8array->buffer()->is_shared()) {
-      simd_result_size = simdutf::atomic_binary_to_base64(
-          std::bit_cast<const char*>(uint8array->GetBuffer()->backing_store()),
-          length, reinterpret_cast<char*>(output->GetChars(no_gc)), alphabet);
+      std::unique_ptr<char[]> tmp = std::make_unique<char[]>(length);
+      base::Relaxed_Memmove(
+          reinterpret_cast<base::Atomic8*>(tmp.get()),
+          reinterpret_cast<base::Atomic8*>(const_cast<char*>(
+              std::bit_cast<const char*>(uint8array->GetBuffer()->backing_store()))),
+          length);
+      simd_result_size = simdutf::binary_to_base64(
+          tmp.get(), length,
+          reinterpret_cast<char*>(output->GetChars(no_gc)), alphabet);
     } else {
       simd_result_size = simdutf::binary_to_base64(
           std::bit_cast<const char*>(uint8array->GetBuffer()->backing_store()),
diff --git a/src/objects/simd.cc b/src/objects/simd.cc
index 3202e51e4f3..bc3d19a497f 100644
--- a/src/objects/simd.cc
+++ b/src/objects/simd.cc
@@ -484,11 +484,17 @@ void AtomicUint8ArrayToHexSlow(const char* bytes, size_t length,
   // https://cplusplus.github.io/LWG/issue3508
   // we instead provide a mutable input, which is ok since we are only reading
   // from it.
-  char* mutable_bytes = const_cast<char*>(bytes);
+  // char* mutable_bytes = const_cast<char*>(bytes);
   for (size_t i = 0; i < length; i++) {
-    uint8_t byte =
-        std::atomic_ref<char>(mutable_bytes[i]).load(std::memory_order_relaxed);
-    PerformNibbleToHexAndWriteIntoStringOutPut(byte, index, string_output);
+     char byte;
+     __atomic_load(
+         const_cast<char*>(bytes) + i,
+         &byte,
+         __ATOMIC_RELAXED
+     );
+    PerformNibbleToHexAndWriteIntoStringOutPut(
+         static_cast<uint8_t>(byte), 
+         index, string_output);
     index += 2;
   }
 }
@@ -1078,14 +1084,21 @@ bool ArrayBufferFromHex(const base::Vector<T>& input_vector, bool is_shared,
 
   size_t index = 0;
   std::optional<uint8_t> result = 0;
-  for (uint32_t i = 0; i < input_length; i += 2) {
+  for (size_t i = 0; i < output_length * 2; i += 2) {
     result = HandleRemainingHexValues(input_vector, i);
     if (result.has_value()) {
+      uint8_t value = result.value();
       if (is_shared) {
-        std::atomic_ref<uint8_t>(buffer[index++])
-            .store(result.value(), std::memory_order_relaxed);
+        // std::atomic_ref<uint8_t>(buffer[index++])
+        //     .store(result.value(), std::memory_order_relaxed);
+         __atomic_store(
+             buffer + index,
+             &value,
+             __ATOMIC_RELAXED
+         );
+         index++;
       } else {
-        buffer[index++] = result.value();
+        buffer[index++] = value;
       }
     } else {
       return false;
